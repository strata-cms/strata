## What changed

## Why

## Verification
- [ ] Focused tests added/updated where appropriate
- [ ] `make architecture-check` (architecture/persistence changes)
- [ ] `make check`
- [ ] `make security` (required for security-sensitive changes)
- [ ] `make package-build` (required for packaging changes)
- [ ] `make docker-build` (required for Docker/runtime changes)

## Impact
- Migrations:
- Public API / OpenAPI compatibility:
- Architecture / persistence boundary:
- Plugin/content schema compatibility:
- Extension points / packaging:
- Security / permissions:
- Operational / deployment:
